app-id: io.github.aaron777collins.zerotier-gui
runtime: org.gnome.Platform
runtime-version: "46"
sdk: org.gnome.Sdk
command: zerotier-gui

modules:
  - name: gcc
    buildsystem: simple
    build-commands:
      - apt-get update -y
      - apt-get install -y gcc g++ make
    sources:
      - type: archive
        url: http://deb.debian.org/debian/pool/main/g/gcc-defaults/gcc_10.2.0-1_amd64.deb
        sha256: c4eb70b1159d6a2a15de354d1ef11c0987875f4b4a8c223bd94654536ad3c7ad
      - type: archive
        url: http://deb.debian.org/debian/pool/main/g/gcc-defaults/g++_10.2.0-1_amd64.deb
        sha256: dfeb0e1f8fa6a93b4d978b50d058d2478c1b3c4c27671f0c140d49c623d3f44e

  - name: python3
    buildsystem: simple
    build-commands:
      - ./configure --prefix=/app --enable-shared --with-system-ffi --with-ensurepip=install
      - make
      - make install
    sources:
      - type: archive
        url: https://www.python.org/ftp/python/3.10.4/Python-3.10.4.tgz
        sha256: f3bcc65b1d5f1dc78675c746c98fcee823c038168fc629c5935b044d0911ad28

  - "python3-tkinter.yaml"

  - name: zerotier-gui
    buildsystem: simple
    build-commands:
      - install -D src/zerotier-gui.py /app/bin/zerotier-gui
      - install -D requirements.txt /app/requirements.txt
      - pip3 install --no-index --find-links="file://${PWD}" -r /app/requirements.txt --prefix=/app
    sources:
      - type: git
        url: https://github.com/aaron777collins/ZeroTier-GUI-Arch.git
        branch: master

finish-args:
  - --share=network
  - --filesystem=host
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --talk-name=org.freedesktop.Flatpak
  - --system-talk-name=org.freedesktop.Flatpak
  - --filesystem=~/.local/share/zerotier-gui
  - --filesystem=/var/lib/zerotier-one:ro
  - --env=PYTHONPATH=/app/lib/python3.10/site-packages:/app/lib/python3.10
  - --env=PYTHONHOME=/app
  - --env=PYTHONUNBUFFERED=1
  - --env=PYTHONNOUSERSITE=1
  - --env=PYTHONEXECUTABLE=/app/bin/python3
  - --env=LD_LIBRARY_PATH=/app/lib
